<Window x:Class="DownloadComics.windows.ChangeSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows"
        xmlns:res="clr-namespace:DownloadComics.Extension"
        mc:Ignorable="d"
        Title="{res:Loc Key=Title, Section=ChangeSourceWindow}" 
        Height="150" Width="350" WindowStartupLocation="CenterOwner"
        DataContext="{Binding ViewModel, RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="10">
            <ComboBox FontSize="16" Width="200" HorizontalAlignment="Center"
                      ItemsSource="{Binding Hosts}"
                      SelectedItem="{Binding Comic.Host, Mode=TwoWay}"
                      SelectionChanged="ComboBox_SelectionChanged"/>
            <Button Content="{res:Loc Key=CloseBTN, Section=Buttons}"
                    Margin="0,10,0,0" Padding="5,0,5,0" FontSize="16"
                    Click="Button_Click" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
