<Window x:Class="DownloadComics.windows.Dialogs.RestoreDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows.Dialogs"
        xmlns:res="clr-namespace:DownloadComics.Extension"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="{res:Loc Key=Title, Section=RestoreDialog}" Height="150" Width="260"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Question, Mode=TwoWay}" TextWrapping="Wrap"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="{res:Loc Key=Yes, Section=Dialog}" Click="ConfirmBTN_Click"
                    FontSize="16" Margin="10,0,10,0" Padding="5,0,5,0"/>
            <Button Content="{res:Loc Key=No, Section=Dialog}" Click="Deny_Click"
                    FontSize="16" Margin="10,0,10,0" Padding="5,0,5,0"/>
        </StackPanel>
    </Grid>
</Window>
