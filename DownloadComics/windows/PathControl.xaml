<Window x:Class="DownloadComics.windows.PathControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows"
        xmlns:res="clr-namespace:DownloadComics.resources"
        mc:Ignorable="d"
        Title="{res:Loc Path_Title}" Height="450" Width="485" WindowStartupLocation="CenterOwner"
        Closing="Window_Closing" Loaded="Window_Loaded" ResizeMode="CanMinimize" SizeToContent="Width">

    <Window.Resources>
        <Style TargetType="ScrollBar" x:Key="ThinHorizontalScrollBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="MinHeight" Value="8"/>
        </Style>
    </Window.Resources>

    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="1" x:Name="pathList" SelectionChanged="PathList_SelectionChanged"/>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="{res:Loc Key=Comic_Path_Label,Format={}{0}:}" VerticalAlignment="Center" Grid.Column="0"/>
                <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="0,0,10,0" VerticalAlignment="Center">
                    <ComboBox Name="rootCB" Width="200" Margin="0,0,10,0"/>
                    <TextBox Name="pathTXT" HorizontalAlignment="Stretch" Width="200" HorizontalScrollBarVisibility="Auto" >
                        <TextBox.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource ThinHorizontalScrollBar}"/>
                        </TextBox.Resources>
                    </TextBox>
                </StackPanel>
                
            </Grid>
        </StackPanel>

        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="selectButton" Width="100" Content="{res:Loc Path_Select_Button}" FontSize="16" 
                    HorizontalAlignment="Center" Click="SelectButton_Click"/>
            <Button Grid.Column="1" Name="cancelButton" Width="100" Content="{res:Loc Path_Cancel_Button}" FontSize="16" 
                    HorizontalAlignment="Center" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
