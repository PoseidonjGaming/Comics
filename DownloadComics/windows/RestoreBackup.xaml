<Window x:Class="DownloadComics.windows.RestoreBackup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows" 
        xmlns:model="clr-namespace:DownloadComics.models" 
        xmlns:res="clr-namespace:DownloadComics.resources"
        mc:Ignorable="d"
        Title="{res:Loc Restore_Title}" Height="450" Width="400" WindowStartupLocation="CenterOwner" Loaded="Window_Loaded"
        ResizeMode="NoResize" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <TreeView Name="folderTree" Grid.Row="1" Margin="0,10,0,0"/>

        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Name="searchBTN" Click="SearchBTN_Click" Content="{res:Loc Restore_Search_Button}" FontSize="16" Margin="0,10,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Name="restoreBtn" Click="RestoreBtn_Click" Content="{res:Loc Restore_Restore_Button}" FontSize="16" Padding="10,0,10,0" Margin="0,0,10,0" />
                <Button Name="deleteBtn" Click="DeleteBtn_Click" Content="{res:Loc Restore_Delete_Button}" FontSize="16" Padding="10,0,10,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
