<Window x:Class="DownloadComics.windows.SendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows"
        mc:Ignorable="d"
        Title="SendWindow" Height="450" Width="400" WindowStartupLocation="CenterOwner"
        DataContext="{Binding ViewModel, RelativeSource={RelativeSource Self}}"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <ToggleButton Margin="10" Content="{Binding Job, Mode=TwoWay}"
                          Checked="JobToggleBTN_Checked" Unchecked="JobToggleBTN_Unchecked"
                          HorizontalAlignment="Center" FontSize="20" Padding="15"/>
            <ProgressBar Height="20" Minimum="0" Value="{Binding ProgressValue, Mode=TwoWay}"
                         IsIndeterminate="{Binding IsDetermined, Mode=TwoWay}"
                         Maximum="{Binding Maximum, Mode=TwoWay}"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding StateJob}" FontSize="16" Margin="10"/>
        <ListBox Grid.Row="2" Margin="10,0,10,0" ItemsSource="{Binding States}"/>
    </Grid>
</Window>
