<Window x:Class="DownloadComics.windows.VerifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadComics.windows"
        xmlns:res="clr-namespace:DownloadComics.resources"
        mc:Ignorable="d"
        Title="VerifyWindows" Height="700" Width="400" WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <ToggleButton Name="jobToggleBtn" Margin="10" Content="{res:Loc Verify_Start_Button}" FontSize="20"
                          Checked="JobToggleBtn_Checked" Unchecked="JobToggleBtn_Unchecked" />
            <ProgressBar Name="progressBar" Height="20" Minimum="0" Maximum="100"/>
        </StackPanel>

        <Label Name="tryLBL" Grid.Row="1" FontSize="16" Margin="10,10,10,0"/>
        <ListBox Grid.Row="2" Name="urlListBox" Margin="10,0,10,10"/>

    </Grid>
</Window>
