<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ModernDownladComics.Pages.SendPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernDownladComics.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:src="using:ComicsLib.Models"
    xmlns:vw="using:ModernDownladComics.Models.View"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vw:SendViewModel}"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <ToggleButton x:Name="jobToggleBTN" Margin="10"
                          Checked="JobToggleBTN_Checked" Unchecked="jobToggleBTN_Unchecked"
                          HorizontalAlignment="Center" FontSize="20" Padding="15"/>
            <ProgressBar Height="20" Minimum="0" Value="{Binding ProgessValue}"
                         IsIndeterminate="{Binding IsDetermined}"
                         Maximum="{x:Bind src:AppStateStore.Instance.Comics.Count}"/>
        </StackPanel>

        <TextBlock Text="{Binding StateJob}" Grid.Row="1" FontSize="16" Margin="10,10,10,5"/>

        <ListBox Grid.Row="2" Margin="10,0,10,0" ItemsSource="{Binding States}"/>
    </Grid>
</Page>
