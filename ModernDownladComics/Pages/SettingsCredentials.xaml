<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ModernDownladComics.Pages.SettingsCredentials"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernDownladComics.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical" Margin="10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Email :" VerticalAlignment="Center" FontSize="16" Margin="0,0,0,5"/>
                <TextBox HorizontalAlignment="Stretch"
                         FontSize="16" TextWrapping="NoWrap"
                         Text="{Binding JDCredentials.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="0,10,0,0">
                <TextBlock Text="Password:" VerticalAlignment="Center" FontSize="16" Margin="0,0,0,5"/>
                <PasswordBox HorizontalAlignment="Stretch" FontSize="16"
                             Password="{Binding JDCredentials.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            </StackPanel>
            <StackPanel Orientation="Vertical"  Grid.Row="1" Margin="0,10,0,0">
                <TextBlock Text="Device Name:" VerticalAlignment="Center" FontSize="16" Margin="0,0,0,5"/>
                <TextBox HorizontalAlignment="Stretch" FontSize="16" TextWrapping="NoWrap"
                         Text="{Binding JDCredentials.Device, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="0,10,0,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Test Connection" FontSize="16"
                            Click="Button_Click"/>
                </StackPanel>
                <TextBlock Name="connectionLBL" FontSize="16"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Page>
