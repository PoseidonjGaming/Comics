<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ModernDownladComics.windows.ChangeSourceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernDownladComics.windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ChangeSourceWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="10">
            <ComboBox x:Name="comicsHostCMB" FontSize="14" Width="150" HorizontalAlignment="Center"
                      SelectedItem="{x:Bind Comic.Host, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{x:Bind Hosts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectionChanged="ComicsHostCMB_SelectionChanged"/>
            <Button Content="Close" Margin="0,10,0,0" Click="SelectBTN_Click" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
